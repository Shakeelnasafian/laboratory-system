services:
  lab-nginx:
    image: nginx:alpine
    ports:
      - "8082:80"
    volumes:
      - .:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      lab-php:
        condition: service_healthy
    networks:
      - shared-network

  # ---- PHP Application ----
  lab-php:
    build:
      context: .
      dockerfile: Dockerfile
    image: laboratory-system-lab-php:latest    # 👈 name the image
    volumes:
      - .:/var/www/html
    networks:
      shared-network:
        aliases:
          - lab-php
    healthcheck:
      test: ["CMD", "php-fpm", "-t"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # ---- Queue Worker ----
  lab-horizon:
    image: laboratory-system-lab-php:latest    # 👈 reuse same image
    command: php artisan queue:listen --tries=3
    volumes:
      - .:/var/www/html
    networks:
      - shared-network

  # ---- Scheduler ----
  lab-scheduler:
    image: laboratory-system-lab-php:latest
    command: sh -c "while true; do php artisan schedule:run; sleep 60; done"
    volumes:
      - .:/var/www/html
    networks:
      - shared-network

networks:
  shared-network:
    external: true
    name: shared-network